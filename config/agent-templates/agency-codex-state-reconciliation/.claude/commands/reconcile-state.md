---
description: Compare onboarding surfaces and classify drift
allowed-tools: Read, Write, Glob, Bash
---

# Reconcile State

Compare runtime pipeline state, shared projections, worker reports, evidence
files, and client-facing artifacts. Classify drift as stale projection, missing
evidence, authority conflict, or resolved.

Save findings under `/home/developer/shared-out/reconciliation/`. Do not
overwrite another agent's owned file.
